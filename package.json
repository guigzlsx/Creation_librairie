{
  "name": "animation-lib",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:e2e": "cypress open",
    "test:e2e:ci": "cypress run",
    "test": "echo 'No unit tests: only E2E tests with Cypress' && exit 0",
    "test:coverage": "echo 'No coverage: only E2E tests with Cypress' && exit 0",
    "format:check": "echo 'No format check: not configured' && exit 0"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.0.0",
    "cypress": "^13.6.6"
  }
}
